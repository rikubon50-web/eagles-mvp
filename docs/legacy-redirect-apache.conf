# agulaxmen.com → aoyamaeagles.com 301転送（Apache用・案Y）
# VirtualHost 内または .htaccess に配置。mod_rewrite が必要。
# 個別記事・選手の精密な対応は新サイト側の転送ルールが引き継ぐため、
# ここではパスをそのまま新ドメインへ渡すだけでよい。

RewriteEngine On
RewriteCond %{HTTP_HOST} ^(www\.)?agulaxmen\.com$ [NC]
RewriteRule ^(.*)$ https://aoyamaeagles.com/$1 [R=301,L]
